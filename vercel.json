{
  "buildCommand": "cd frontend/dashboard-admin && npm install && npm run build",
  "outputDirectory": "frontend/dashboard-admin/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}