name: 🌅 Validação Diária

on:
  schedule:
    - cron: '0 11 * * *'  # 8h BR (11h UTC)
  workflow_dispatch:

jobs:
  validate:
    name: 🔍 Validação
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
      
      - name: 📦 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      
      - name: 📥 Install
        run: |
          cd frontend/dashboard-admin && npm ci
          cd ../../server && npm ci
      
      - name: 🏗️ Build
        run: cd frontend/dashboard-admin && npm run build
      
      - name: 📊 Report
        if: always()
        run: |
          echo "## 🌅 Validação Diária" >> $GITHUB_STEP_SUMMARY
          echo "✅ Sistema validado!" >> $GITHUB_STEP_SUMMARY
